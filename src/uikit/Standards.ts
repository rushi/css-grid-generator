export type VariantNames = "standard" | "outline" | "link";

export enum Variant {
    STANDARD = "standard",
    OUTLINE = "outline",
    LINK = "link",
}

export type ColorNames = "primary" | "secondary" | "success" | "danger" | "warning" | "caution";

export enum Color {
    PRIMARY = "primary",
    SECONDARY = "secondary",
    SUCCESS = "success",
    DANGER = "danger",
    WARNING = "warning",
    CAUTION = "caution",
}

export type SizeNames = "tiny" | "small" | "medium" | "large";

export enum Size {
    TINY = "tiny",
    SMALL = "small",
    MEDIUM = "medium",
    LARGE = "large",
}
